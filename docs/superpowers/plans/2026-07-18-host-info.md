# 系统信息展示 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 连接列表新增"信息"列，连接成功后自动采集 Linux 服务器的系统发行版、CPU 核数、内存、硬盘信息并缓存，支持手动刷新。

**Architecture:** 主进程新建 `sysInfoCollector` 模块：连接成功后复用当前 SSH 会话执行 4 条 Linux 命令（os-release / nproc / free / df）采集，解析后写回连接配置的 `sysInfo` 字段；渲染进程 HostTable 新增"信息"列直接读缓存展示，行尾放刷新按钮支持手动重采。

**Tech Stack:** Electron 31 + React 18 + TypeScript 5.5 + ssh2 + electron-vite

**Spec:** `docs/superpowers/specs/2026-07-18-host-info-design.md`

---

## File Structure

| 文件 | 职责 |
| --- | --- |
| `src/shared/types.ts` | `SSHConnection.sysInfo` 字段、`EasyShellApi.ssh.collectInfo` / `onConnectionsChanged` 接口 |
| `src/main/sysInfoCollector.ts` | 新建：采集逻辑（连接复用/新建、命令执行、解析、写回） |
| `src/main/sshManager.ts` | 新增 `getClientByConnectionId(connectionId)`，供复用现有连接 |
| `src/main/ipc.ts` | 注册 `ssh:collectInfo`；`ssh:connect` 成功后自动触发采集；采集完成后推送 `connections:changed` |
| `src/preload/index.ts` | 暴露 `ssh.collectInfo` 和 `ssh.onConnectionsChanged` |
| `src/renderer/src/mockApi.ts` | mock 假 sysInfo 数据 + collectInfo 假实现 |
| `src/renderer/src/components/HostTable.tsx` | 加"信息"列 + 刷新按钮 + 加载态 |
| `src/renderer/src/styles.css` | 信息列样式 + 刷新按钮转圈动画 |
| `src/renderer/src/App.tsx` | 订阅 `connections:changed`，触发列表刷新 |

---

### Task 1: 共享类型扩展

**Files:**
- Modify: `src/shared/types.ts`

- [ ] **Step 1: 给 SSHConnection 加 sysInfo 字段**

在 `SSHConnection` 接口内，`lastConnectedAt?: number` 之后加：

```ts
  sysInfo?: {
    os: string        // 如 'Ubuntu 22.04'
    cpuCores: number  // 如 8
    memTotal: string  // 如 '15G'
    diskTotal: string // 如 '39G'
    collectedAt: number
  } | null
```

- [ ] **Step 2: EasyShellApi.ssh 加两个方法**

在 `EasyShellApi` 接口的 `ssh: { ... }` 组内，`test(...)` 之后加：

```ts
    collectInfo(connectionId: string): Promise<{ ok: boolean; error?: string }>
    onConnectionsChanged(cb: () => void): () => void
```

- [ ] **Step 3: 提交**

```bash
git add src/shared/types.ts
git commit -m "feat: SSHConnection 加 sysInfo 缓存字段与采集接口形状"
```

---

### Task 2: 主进程 sysInfoCollector 模块

**Files:**
- Create: `src/main/sysInfoCollector.ts`
- Modify: `src/main/sshManager.ts`

- [ ] **Step 1: sshManager 加 getClientByConnectionId**

在 `src/main/sshManager.ts` 中，`Session` 接口改为：

```ts
interface Session {
  id: string
  connectionId: string
  client: Client
  stream: NodeJS.ReadWriteStream | null
}
```

`connect` 函数里的 `const session: Session = { id: sessionId, client, stream: null }` 改为：

```ts
const session: Session = {
  id: sessionId,
  connectionId: conn.id,
  client,
  stream: null
}
```

在 `getClient(sessionId)` 之后追加：

```ts
export function getClientByConnectionId(connectionId: string): Client | null {
  for (const s of sessions.values()) {
    if (s.connectionId === connectionId) return s.client
  }
  return null
}
```

- [ ] **Step 2: 创建 sysInfoCollector.ts**

完整内容：

```ts
import { Client } from 'ssh2'
import type { SSHConnection } from '../shared/types'
import * as store from './store'
import * as ssh from './sshManager'
import fs from 'fs'

interface SysInfoResult {
  os: string
  cpuCores: number
  memTotal: string
  diskTotal: string
  collectedAt: number
}

// 在 client 上执行一条命令，返回 stdout 文本；失败抛错
function execOn(client: Client, cmd: string): Promise<string> {
  return new Promise((resolve, reject) => {
    client.exec(cmd, (err, stream) => {
      if (err) return reject(err)
      let out = ''
      let errOut = ''
      stream.on('data', (d: Buffer) => {
        out += d.toString('utf-8')
      })
      stream.stderr?.on('data', (d: Buffer) => {
        errOut += d.toString('utf-8')
      })
      stream.on('close', (code: number) => {
        if (code !== 0) {
          return reject(new Error(errOut.trim() || `命令退出码 ${code}`))
        }
        resolve(out)
      })
    })
  })
}

function parseOs(text: string): string {
  const m = text.match(/PRETTY_NAME="?([^"\n]+)"?/)
  return m ? m[1].trim() : '-'
}

function parseCpu(text: string): number {
  const n = parseInt(text.trim(), 10)
  return Number.isFinite(n) && n > 0 ? n : 0
}

function parseMem(text: string): string {
  // 取 Mem 行第二列（MB 总量）
  const line = text.split('\n').find((l) => l.startsWith('Mem:'))
  if (!line) return '-'
  const mb = parseInt(line.split(/\s+/)[1], 10)
  if (!Number.isFinite(mb)) return '-'
  const gb = mb / 1024
  return gb >= 1 ? `${Math.round(gb)}G` : `${mb}M`
}

function parseDisk(text: string): string {
  // df -h / 第二行第二列是总量，如 "39G"
  const lines = text.trim().split('\n')
  if (lines.length < 2) return '-'
  const parts = lines[1].split(/\s+/)
  return parts[1] || '-'
}

async function runCollect(client: Client): Promise<SysInfoResult> {
  const [osRaw, cpuRaw, memRaw, diskRaw] = await Promise.all([
    execOn(client, 'cat /etc/os-release').catch(() => ''),
    execOn(client, 'nproc').catch(() => ''),
    execOn(client, 'free -m').catch(() => ''),
    execOn(client, 'df -h /').catch(() => '')
  ])
  return {
    os: osRaw ? parseOs(osRaw) : '-',
    cpuCores: cpuRaw ? parseCpu(cpuRaw) : 0,
    memTotal: memRaw ? parseMem(memRaw) : '-',
    diskTotal: diskRaw ? parseDisk(diskRaw) : '-',
    collectedAt: Date.now()
  }
}

// 复用现有连接执行采集；没有则建临时连接
export async function collectSystemInfo(
  conn: SSHConnection
): Promise<{ ok: boolean; error?: string }> {
  let client: Client | null = ssh.getClientByConnectionId(conn.id)
  let ownClient = false
  if (!client) {
    client = new Client()
    ownClient = true
    await new Promise<void>((resolve, reject) => {
      const cfg = buildClientConfig(conn)
      client!.once('ready', () => resolve())
      client!.once('error', (err) =>
        reject(new Error(err.message || '连接失败'))
      )
      try {
        client!.connect(cfg)
      } catch (e) {
        reject(e instanceof Error ? e : new Error(String(e)))
      }
    })
  }
  try {
    const info = await runCollect(client)
    store.updateConnection(conn.id, { sysInfo: info })
    return { ok: true }
  } catch (e) {
    return { ok: false, error: e instanceof Error ? e.message : String(e) }
  } finally {
    if (ownClient) {
      try {
        client.end()
      } catch {
        /* ignore */
      }
    }
  }
}

// 与 sshManager.buildConfig 保持一致（避免循环依赖，此处复制一份）
function buildClientConfig(conn: SSHConnection): Parameters<Client['connect']>[0] {
  const cfg: Parameters<Client['connect']>[0] = {
    host: conn.host,
    port: conn.port || 22,
    username: conn.username,
    readyTimeout: conn.timeout && conn.timeout > 0 ? conn.timeout : 10000,
    keepaliveInterval:
      conn.heartbeat && conn.heartbeat > 0 ? conn.heartbeat : 5000,
    keepaliveCountMax: 3
  }
  if (conn.authType === 'privateKey' && conn.privateKeyPath) {
    cfg.privateKey = fs.readFileSync(conn.privateKeyPath)
    if (conn.passphrase) cfg.passphrase = conn.passphrase
  } else {
    cfg.password = conn.password || ''
  }
  return cfg
}
```

- [ ] **Step 3: 类型检查**

Run: `npx tsc -b`
Expected: 通过（preload/mockApi 可能报缺 ssh.collectInfo/onConnectionsChanged 的错，预期内，下个任务补）

- [ ] **Step 4: 提交**

```bash
git add src/main/sysInfoCollector.ts src/main/sshManager.ts
git commit -m "feat: 主进程系统信息采集（复用连接或建临时连接执行 4 条命令）"
```

---

### Task 3: IPC + preload 暴露

**Files:**
- Modify: `src/main/ipc.ts`
- Modify: `src/preload/index.ts`

- [ ] **Step 1: ipc.ts 注册**

顶部 import 加：

```ts
import * as sysInfo from './sysInfoCollector'
```

在 `registerIpc` 函数内，`ssh:test` 注册之后追加：

```ts
  ipcMain.handle('ssh:collectInfo', async (_e, connectionId: string) => {
    const conn = store.getConnection(connectionId)
    if (!conn) throw new Error('连接不存在')
    const result = await sysInfo.collectSystemInfo(conn)
    // 采集完成后推一次，让列表刷新
    getWindow()?.webContents.send('connections:changed')
    return result
  })
```

在 `ssh:connect` handler 里，`store.updateConnection(connectionId, { lastConnectedAt: Date.now() })` 之后追加自动采集（fire-and-forget，不阻塞终端打开）：

```ts
      sysInfo.collectSystemInfo(conn).then(() => {
        getWindow()?.webContents.send('connections:changed')
      })
```

- [ ] **Step 2: preload 暴露**

顶部 import 区：

```ts
import type { EasyShellApi, SSHConnection, SftpProgress } from '../shared/types'
```

（无变化，只是确认下）

在 api 对象的 `ssh: { ... }` 组内，`test: (...)` 之后加：

```ts
    collectInfo: (connectionId) =>
      ipcRenderer.invoke('ssh:collectInfo', connectionId),
    onConnectionsChanged: (cb) => {
      const listener = (): void => cb()
      ipcRenderer.on('connections:changed', listener)
      return () => ipcRenderer.removeListener('connections:changed', listener)
    },
```

- [ ] **Step 3: 类型检查**

Run: `npx tsc -b`
Expected: 只剩 mockApi.ts 缺 collectInfo/onConnectionsChanged 的错（预期内）

- [ ] **Step 4: 提交**

```bash
git add src/main/ipc.ts src/preload/index.ts
git commit -m "feat: 系统信息 IPC 注册与 preload 暴露（含自动采集推送）"
```

---

### Task 4: mockApi 补 sysInfo 假数据

**Files:**
- Modify: `src/renderer/src/mockApi.ts`

- [ ] **Step 1: mock 初始连接带 sysInfo**

在 `createMockApi` 返回对象的 `connections.list` 前加一个 mock 数据初始化函数，放在 `createMockApi` 函数体开头：

```ts
  // 首次启动塞两个带 sysInfo 的 mock 连接，方便调试
  if (load().connections.length === 0) {
    save({
      groups: [],
      connections: [
        {
          id: crypto.randomUUID(),
          name: 'Ubuntu 生产机',
          groupId: null,
          host: '10.0.0.8',
          port: 22,
          authType: 'password',
          username: 'root',
          createdAt: Date.now(),
          sysInfo: {
            os: 'Ubuntu 22.04',
            cpuCores: 8,
            memTotal: '15G',
            diskTotal: '39G',
            collectedAt: Date.now() - 3600_000
          }
        },
        {
          id: crypto.randomUUID(),
          name: 'CentOS 测试机',
          groupId: null,
          host: '10.0.0.9',
          port: 22,
          authType: 'password',
          username: 'root',
          createdAt: Date.now(),
          sysInfo: null
        }
      ]
    })
  }
```

- [ ] **Step 2: mock ssh.collectInfo + onConnectionsChanged**

在 `createMockApi` 返回对象的 `ssh: { ... }` 组内，`test: async () => {...}` 之后加：

```ts
      collectInfo: async (connectionId) => {
        await delay(800)
        const d = load()
        const idx = d.connections.findIndex((c) => c.id === connectionId)
        if (idx === -1) return { ok: false, error: '连接不存在' }
        d.connections[idx] = {
          ...d.connections[idx],
          sysInfo: {
            os: 'Ubuntu 22.04',
            cpuCores: 4,
            memTotal: '7G',
            diskTotal: '19G',
            collectedAt: Date.now()
          }
        }
        save(d)
        return { ok: true }
      },
      onConnectionsChanged: () => () => {}
```

- [ ] **Step 3: 类型检查**

Run: `npx tsc -b`
Expected: 全部通过

- [ ] **Step 4: 提交**

```bash
git add src/renderer/src/mockApi.ts
git commit -m "feat: 浏览器 mock 环境假系统信息数据"
```

---

### Task 5: HostTable 加"信息"列 + 刷新按钮

**Files:**
- Modify: `src/renderer/src/components/HostTable.tsx`
- Modify: `src/renderer/src/styles.css`

- [ ] **Step 1: HostTable 加 sysInfo 渲染 + 刷新逻辑**

在 `HostTable.tsx` 中：

1. 顶部 `useState` 区域加刷新中状态：

```tsx
  const [collecting, setCollecting] = useState<Record<string, boolean>>({})
  const [collectError, setCollectError] = useState<Record<string, string>>({})
```

2. 加刷新函数（放在 `testOne` 之后）：

```tsx
  const refreshInfo = async (c: SSHConnection): Promise<void> => {
    if (collecting[c.id]) return
    setCollecting((p) => ({ ...p, [c.id]: true }))
    setCollectError((p) => ({ ...p, [c.id]: '' }))
    const r = await api.ssh.collectInfo(c.id)
    if (!r.ok) {
      setCollectError((p) => ({
        ...p,
        [c.id]: r.error || '采集失败'
      }))
    }
    setCollecting((p) => ({ ...p, [c.id]: false }))
    // 让外层列表刷新（展示新 sysInfo）
    props.onRefresh?.()
  }
```

3. Props 接口加可选的 `onRefresh`：

```tsx
interface Props {
  connections: SSHConnection[]
  onConnect: (c: SSHConnection) => void
  onEdit: (c: SSHConnection) => void
  onRemove: (c: SSHConnection) => void
  onToggleFav: (c: SSHConnection) => void
  onRefresh?: () => void
}
```

4. 表头加一列（在 `<th style={{ width: 110 }}>延迟</th>` 之后）：

```tsx
            <th style={{ width: 220 }}>信息</th>
```

5. 表格 body 里，延迟单元格 `</td>` 之后插入"信息"单元格：

```tsx
                <td>
                  <div className="sys-info-cell">
                    {collecting[c.id] ? (
                      <span className="sys-info loading">采集ing…</span>
                    ) : c.sysInfo ? (
                      <span className="sys-info">
                        {c.sysInfo.os} · {c.sysInfo.cpuCores}核 ·{' '}
                        {c.sysInfo.memTotal} · {c.sysInfo.diskTotal}
                      </span>
                    ) : (
                      <span
                        className="sys-info empty"
                        title={
                          collectError[c.id]
                            ? `采集失败：${collectError[c.id]}`
                            : '未采集'
                        }
                      >
                        -
                      </span>
                    )}
                    <button
                      className={`sys-info-refresh ${collecting[c.id] ? 'spinning' : ''}`}
                      title="刷新系统信息"
                      disabled={collecting[c.id]}
                      onClick={() => refreshInfo(c)}
                    >
                      ⟳
                    </button>
                  </div>
                </td>
```

- [ ] **Step 2: styles.css 加样式**

在文件末尾追加：

```css

/* ---------- 系统信息列 ---------- */
.sys-info-cell {
  display: flex;
  align-items: center;
  gap: 6px;
  font-size: 12px;
}

.sys-info {
  color: var(--text-dim);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
  max-width: 180px;
}

.sys-info.empty {
  color: var(--text-faint);
}

.sys-info.loading {
  color: var(--text-dim);
  font-style: italic;
}

.sys-info-refresh {
  padding: 2px 4px;
  border-radius: 4px;
  color: var(--text-dim);
  font-size: 12px;
  line-height: 1;
  flex-shrink: 0;
}

.sys-info-refresh:hover:not(:disabled) {
  background: var(--bg-hover);
  color: var(--text);
}

.sys-info-refresh:disabled {
  cursor: not-allowed;
  opacity: 0.5;
}

.sys-info-refresh.spinning {
  animation: spin 0.8s linear infinite;
}

@keyframes spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}
```

- [ ] **Step 3: 类型检查**

Run: `npx tsc -b`
Expected: 通过

- [ ] **Step 4: 提交**

```bash
git add src/renderer/src/components/HostTable.tsx src/renderer/src/styles.css
git commit -m "feat: 连接列表加系统信息列（自动采集 + 手动刷新）"
```

---

### Task 6: App.tsx 订阅 connections:changed

**Files:**
- Modify: `src/renderer/src/App.tsx`

现状：App.tsx 已有 `const reload = useCallback(...)`（第 24 行），两个 useEffect 分别订阅初始加载和（此处新增）connections:changed。

- [ ] **Step 1: 订阅推送刷新列表**

在 App.tsx 中，现有这个 useEffect 之后（约第 35 行）：

```tsx
  useEffect(() => {
    reload()
  }, [reload])
```

追加：

```tsx
  useEffect(() => {
    const off = api.ssh.onConnectionsChanged(() => {
      reload()
    })
    return off
  }, [reload])
```

- [ ] **Step 2: HostTable 传入 onRefresh**

找到渲染 `<HostTable ... />` 的地方（约第 198 行），在 `onToggleFav={handleToggleFav}` 后加一个 prop：

```tsx
            onRefresh={reload}
```

- [ ] **Step 3: 类型检查**

Run: `npx tsc -b`
Expected: 通过

- [ ] **Step 4: 提交**

```bash
git add src/renderer/src/App.tsx
git commit -m "feat: 订阅采集完成推送，自动刷新连接列表"
```

---

### Task 7: 全量验证

**Files:**
- 无新文件，只跑现有应用验证

- [ ] **Step 1: 类型 + 构建检查**

```bash
npx tsc -b
npm run build
```

Expected: 全部通过

- [ ] **Step 2: 浏览器 mock 环境验证**

1. `npm run dev` 启动，浏览器打开 http://localhost:5173
2. 预期效果：
   - 列表展示"信息"列，第一条 Ubuntu 生产机显示 `Ubuntu 22.04 · 8核 · 15G · 39G`
   - 第二条 CentOS 测试机显示 `-`
   - 点 CentOS 行的 ⟳ 按钮，1 秒后变成 `Ubuntu 22.04 · 4核 · 7G · 19G`（mock 采集返回假数据），按钮期间转圈
   - 未采集时悬停 `-` 显示"未采集"

- [ ] **Step 3: 真机验证（可选）**

如果有可连的 Linux 服务器：
1. 点连接进入终端，回到列表页，"信息"列应自动出现真实系统信息
2. 点 ⟳ 手动刷新，信息更新
3. 服务器关机后点 ⟳，旧信息保留，悬停提示失败原因

- [ ] **Step 4: 完成提交**

```bash
git push origin main
```

---

## 测试检查清单

- [ ] `npx tsc -b` 零错误
- [ ] `npm run build` 零错误
- [ ] mock 列表两列分别显示有/无 sysInfo 两种状态
- [ ] 手动点 ⟳ 能触发 mock 采集并更新显示
- [ ] 采集期间按钮禁用 + 转圈动画
- [ ] 采集失败悬停有提示
